Category 


<% @category.each do |category| %>
  <%= category.title %>
  <% category.items.each do |item| %> 
    <%= item.name %>
  <% end %>
  <% if current_user.try(:admin?) %>
    <%= link_to 'Add Items To This Category', category_path(category.id) %>
  <% end %>  
<% end %> 

